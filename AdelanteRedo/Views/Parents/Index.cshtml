@model IEnumerable<DataLib.Parent>

@{
    ViewBag.Title = "Index";
}

<br />
<br />
<br />
<br />
<br />
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Parent_FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Parent_LastName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Parent_FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Parent_LastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Parent_NUM }) |
            @Html.ActionLink("Details", "Details", new { id=item.Parent_NUM }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Parent_NUM })
        </td>
    </tr>
}

</table>
